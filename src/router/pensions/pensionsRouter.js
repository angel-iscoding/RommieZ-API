import { Router } from "express";

const pensionsRouter = Router();

pensionsRouter.get('/:id',);

pensionsRouter.post('/',);

pensionsRouter.put(':id',);

pensionsRouter.delete(':id',)

export default pensionsRouter;